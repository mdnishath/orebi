import React from "react";

const Heading = ({ title }) => {
  return (
    <h2 className="font-dm text-2xl font-bold text-primary md:text-[39px]">
      {title}
    </h2>
  );
};

export default Heading;
